{
  "name": "coverdiario",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "dev":   "node scripts/build.js && npx serve -l 3456 ."
  },
  "engines": {
    "node": ">=18"
  }
}
